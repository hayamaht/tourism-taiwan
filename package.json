{
  "name": "tourism-taiwan",
  "version": "1.0.0",
  "engines": {
    "node": ">=18 <19"
  },
  "scripts": {
    "dev": "npm run dev:front && npm run dev:back",
    "dev:front": "cd frontend && npm start",
    "dev:back": "cd backend && npm start ",
    "start": "cd backend/built && node server.js",
    "prebuild": "cd backend && npm run build",
    "build": "cd frontend && npm run build"
  }
}